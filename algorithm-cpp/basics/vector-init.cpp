#include<bits/stdc++.h>

using namespace std;

int main() {
    //vector 초기화 하기
    vector<int> v1[10]; // v벡터를 10개를 생성합니다.
    vector<int> v2(10, 0); // 0이라는 value를 가진 n개의 벡터를 생성 //vector로 2차원 배열 매트릭스 만들기
    vector<vector<int> > checked;
    vector <vector<int> > v(10 + 1 , vector<int> (10 + 1, 0));
}